faster-whisper>=1.0.0
pyyaml>=6.0
playwright>=1.40.0

# 선택: analyze.backend 설정에 따라 필요
# anthropic>=0.30.0          # backend: claude
# google-generativeai>=0.5   # backend: gemini
# ollama>=0.2                # backend: ollama
